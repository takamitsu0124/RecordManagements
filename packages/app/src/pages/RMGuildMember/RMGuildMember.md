# RMGuildMember 仕様書

- 対応ファイル: `src/pages/RMGuildMember/RMGuildMember.vue`
- ルート: `/RMGuildMember`
- レイアウト: `layoutAfterLoginNavigator.vue`
- ステータス: スタブ

## 画面概要
ギルドメンバー用ページとしてルートは定義されていますが、実装はプレースホルダー段階です。現状は固定文字列を 1 行表示するだけです。

## 主な表示要素
- `RMGuildMember` というテキスト

## 主な操作
- なし

## 備考
- 実運用仕様は未着手です。
- 画面の役割が `RMGuildDetail` と重複する可能性があるため、今後の整理方針を先に決める必要があります。
